# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
venv/
.venv/
.mypy_cache/
.build_cache/
.pytest_cache/

# Publisher scratch dirs — these survive crashed or interrupted builds and must
# never be committed. Eight of them (36 GB) were committed before this rule.
.tmp_manual_build_*/

# Local-only setup guide — never commit (contains internal infra details)
DEPLOY_SETUP.md

# Makai Labs internal working docs — never commit
.makai/

# Agent execution workspace (scratch)
.superpowers/
